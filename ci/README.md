# CI

This directory contains CI/CD pipeline configuration.

Responsibilities:
- model analysis execution
- metadata validation
- quality gates before merge

The pipeline operates only on repository artifacts
and does not connect to external systems.
