# SQL

This directory contains all SQL scripts used in the project.

Structure:
- dim/   — dimension tables
- fact/  — fact tables
- views/ — semantic and helper views

All SQL must be compatible with Power BI
(Import mode or DirectQuery).
